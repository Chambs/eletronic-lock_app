apiVersion: v1
kind: Service
metadata:
  name: event-bus
  namespace: electronic-lock-app
spec:
  selector:
    app: event-bus
  ports:
  - port: 10000
    targetPort: 10000
    protocol: TCP
  type: ClusterIP
